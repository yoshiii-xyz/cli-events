# Changelog

## 0.1.0

- Added a versioned JSON Lines schema for command start, output, and finish events.
- Added a bounded Linux-first reference runner with timeout and cancellation state.
- Added stream validation, deterministic summaries, CLI tests, and fuzz coverage.
